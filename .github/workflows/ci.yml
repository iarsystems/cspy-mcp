name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  unit:
    name: Unit tests (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements-dev.txt

      - name: Validate Python sources
        run: python -m compileall -q mcp_thrift_server examples scripts tests

      - name: Run unit tests
        run: pytest -q tests/test_server_tools_unit.py

  live-sim:
    name: Live simulator tests (cxarm)
    runs-on: ubuntu-latest
    needs: unit
    env:
      CXARM_VERSION: "10.10.2"
      # Optional: provide an IAR license bearer token as a repository secret
      # if the toolchain requires license checkout in CI.
      IAR_LMS_BEARER_TOKEN: ${{ secrets.IAR_LMS_BEARER_TOKEN }}
      THRIFT_CSPYSERVER_START_TIMEOUT_MS: "60000"
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements-dev.txt

      - name: Cache cxarm toolchain
        id: cache-cxarm
        uses: actions/cache@v4
        with:
          path: ~/iar/cxarm
          key: cxarm-${{ env.CXARM_VERSION }}-linux-x86_64-base

      - name: Download and extract cxarm
        if: steps.cache-cxarm.outputs.cache-hit != 'true'
        run: |
          mkdir -p ~/iar/cxarm
          curl -fsSL -o /tmp/cxarm.tar.bz2 \
            "https://github.com/iarsystems/arm/releases/download/${CXARM_VERSION}/cxarm-${CXARM_VERSION}-linux-x86_64-base.tar.bz2"
          tar -xjf /tmp/cxarm.tar.bz2 -C ~/iar/cxarm
          rm /tmp/cxarm.tar.bz2

      - name: Locate CSpyServer2
        id: cspy
        run: |
          CSPY="$(find ~/iar/cxarm -type f -name 'CSpyServer2' | head -n 1)"
          if [ -z "$CSPY" ]; then
            echo "CSpyServer2 not found in extracted toolchain:" >&2
            find ~/iar/cxarm -maxdepth 3 -type d >&2
            exit 1
          fi
          chmod +x "$CSPY"
          echo "path=$CSPY" >> "$GITHUB_OUTPUT"
          "$CSPY" --version || true

      - name: Run live simulator tests (runs bundled test.out ELF)
        run: >
          pytest -q --runxfail -m live tests
          --cspyserver2 "${{ steps.cspy.outputs.path }}"

      - name: Run end-to-end demo against bundled ELF
        env:
          THRIFT_CSPYSERVER_EXE: ${{ steps.cspy.outputs.path }}
        run: python examples/run_live_demo.py

      - name: Upload CSpyServer2 logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: cspyserver2-logs
          path: /tmp/cspyserver2-*.log
          if-no-files-found: ignore
